services:
  mysql:
    image: mysql:8.0
    container_name: autoescola-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: fiap
      MYSQL_DATABASE: autoescola3esa
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
